# Build staging + generated output (regenerated by `node pack.mjs`).
standalone/
lib/

# Installed build tooling + local pnpm store/cache.
node_modules/
.pnpm-store/

# Packed distribution artifact (regenerated on demand).
*.tgz

# Editor / OS noise.
.vscode/
.idea/
.DS_Store
Thumbs.db
