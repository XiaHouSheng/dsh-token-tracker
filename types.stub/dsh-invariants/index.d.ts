/** Build-time ambient stub for `@deepseek-ai/dsh-invariants`; never shipped. */

/** Signature of a package invariant installer. The plugin ships a no-op. */
export type InvariantInstaller = (ctx: unknown) => unknown
