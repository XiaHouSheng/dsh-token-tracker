/**
 * Build-time ambient stub for peer packages the plugin references only with
 * empty `import type`/side-effect imports (`@deepseek-ai/dsh-session-query`,
 * `@deepseek-ai/dsh-host-webserver`, `@deepseek-ai/dsh-token-meter`).
 * Never shipped.
 */
export {}
